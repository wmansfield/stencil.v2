<?xml version="1.0" encoding="UTF-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:local="clr-namespace:Stencil.Maui.Views.Standard.v1_0"
                    x:Class="Stencil.Maui.Views.Standard.v1_0.GroupEnd">
    <DataTemplate x:Key="groupEnd" x:DataType="local:GroupEndContext">
        <!--<ViewCell>-->
        <!-- only grid allows pixel perfection adjustments cross platform -->
        <Grid HeightRequest="20"
              ColumnSpacing="0"
              RowSpacing="0"
              Margin="{Binding UIMargin}"
              BackgroundColor="Transparent"
              HorizontalOptions="Fill">
            <Border StrokeThickness="0"
                    Margin="-1,-2,-1,0"
                    BackgroundColor="{Binding InnerColor, Converter={StaticResource stringToColorConverter}}"
                    HorizontalOptions="Fill">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="0,0,10,10" />
                </Border.StrokeShape>
                <Label Text=" "/>
            </Border>
        </Grid>
        <!--</ViewCell>-->
    </DataTemplate>
</ResourceDictionary>